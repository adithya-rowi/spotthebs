import React from "react";
import { createRoot } from "react-dom/client";
import RedFlag from "./RedFlag.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RedFlag />
  </React.StrictMode>
);
